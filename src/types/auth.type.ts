export type signinFormValues = {
  email: string;
  password: string;
  confirmPassword?: string;
  rememberMe?: boolean;
};

export type signupFormValues = {
  name: string;
  company: string;
  jobTitle: string;
};
